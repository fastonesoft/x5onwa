<!--pages/index/form_check.wxml-->
<view class="weui-toptips weui-toptips_warn" wx:if="{{errorShow}}">{{errorMessage}}</view>

<comCheck url="{{url}}" checks="{{checks}}" title="{{title}}" key="{{key}}" memo="{{memo}}" checked="{{checked}}" bind:checkChange="checkChange" />
