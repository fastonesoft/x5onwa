<!--pages/index/schclass.wxml-->

<comRadio radios="{{radios}}" title="学校年级" notitle="{{true}}" key="step_name" memo="grade_name" bind:radioChange="radioChange" />

<comMemb membs="{{membs}}" key="cls_name" split=" - " memo="cls_order" bind:editClick="editClick" bind:removeClick="removeClick" />

<view class="weui-cells__tips x5ui-mini" wx:if="{{grade_id}}">
    <button type="primary" size="mini" bindtap="setClick">设置</button>
    <button type="primary" size="mini" bindtap="addClick">添加</button>
</view>