<!--pages/index/schclassgroup.wxml-->

<comRadio radios="{{radios}}" title="班级分组" key="step_name" memo="grade_name" bind:radioChange="radioChange" />

<comRadio radios="{{groups}}" key="name" bind:radioChange="groupChange" />

<comMemb membs="{{membs}}" key="name" bind:removeClick="removeClick" />

<view class="weui-cells__tips x5ui-mini" wx:if="{{grade_group_id}}">
    <button type="primary" size="mini" bindtap="setClick">设置</button>
</view>

<view class="weui-btn-area">
	<button type="primary" bindtap="returnClick">返回</button>
</view>